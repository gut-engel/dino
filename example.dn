const bool boolean = true;

if (boolean) {
    console.log();
    console.warn();
    console.error();
};

for (var i = 0; i<10; i++) {
    switch (i) {
        case (i<3) {
            
        };
    };
};

console.do($"command {boolean}");